{
  "name": "platybrawl",
  "private": true,
  "version": "0.5.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "playtest": "node scripts/playtest.mjs",
    "sprites": "python3 scripts/process_sprites.py",
    "deploy": "npm run build && wrangler deploy"
  },
  "dependencies": {
    "@fontsource/press-start-2p": "^5.3.0"
  },
  "devDependencies": {
    "puppeteer-core": "^25.6.0",
    "typescript": "~6.0.2",
    "vite": "^8.2.0",
    "vitest": "^4.1.10",
    "wrangler": "^4.122.0"
  }
}
